(executables
 ((names (example))
  (libraries (virtual_dom async_js core_kernel async_kernel js_of_ocaml))
  (preprocess (pps (js_of_ocaml.ppx.driver ppx_sexp_conv)))
  (js_of_ocaml ())
  ))

(jbuild_version jane_street)
