(executables
 ((names (example))
  (libraries (virtual_dom async_js core_kernel async_kernel incr_dom_widgets.css js_of_ocaml))
  (preprocess (pps (js_of_ocaml-ppx ppx_sexp_conv ppxlib.runner)))
  ))


(jbuild_version 1)
