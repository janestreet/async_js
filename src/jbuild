(library
 ((name async_js)
  (public_name async_js)
  (inline_tests ((javascript build_and_run) (native dont_build_dont_run)))
  (libraries (js_of_ocaml
              async_kernel
              async_rpc_kernel
              ))
  (js_of_ocaml ())
  (preprocess (pps (ppx_jane js_of_ocaml.ppx.driver)))))

(jbuild_version jane_street)
